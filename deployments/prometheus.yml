# Prometheus 配置文件

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'llmproxy'
    static_configs:
      - targets: ['llmproxy:8080']
    metrics_path: '/metrics'
