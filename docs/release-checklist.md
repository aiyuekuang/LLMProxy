# 发布检查清单

在发布新版本前，请确保完成以下步骤：

## 📋 发布前检查

### 代码质量
- [ ] 所有测试通过
- [ ] 代码已通过 lint 检查
- [ ] 无已知的严重 bug
- [ ] 关键功能已手动测试

### 文档更新
- [ ] README.md 已更新（如有 API 变更）
- [ ] CHANGELOG.md 已添加版本记录
- [ ] 配置文件示例已更新
- [ ] API 文档已同步

### 版本号确定
- [ ] 确定版本号（遵循 SemVer）
  - 破坏性变更 → MAJOR 版本
  - 新功能 → MINOR 版本
  - Bug 修复 → PATCH 版本

## 🚀 发布步骤

### 1. 更新版本信息

```bash
# 创建 CHANGELOG 条目（如果还没有）
echo "## [1.0.0] - $(date +%Y-%m-%d)" >> CHANGELOG.md
echo "" >> CHANGELOG.md
echo "### Added" >> CHANGELOG.md
echo "- 新功能描述" >> CHANGELOG.md
echo "" >> CHANGELOG.md
echo "### Fixed" >> CHANGELOG.md
echo "- Bug 修复描述" >> CHANGELOG.md
```

### 2. 提交变更

```bash
git add .
git commit -m "chore: release v1.0.0"
git push origin main
```

### 3. 创建并推送 Tag

```bash
# 创建带注释的 tag
git tag -a v1.0.0 -m "Release v1.0.0

主要变更：
- 新功能 A
- 修复 Bug B
- 性能优化 C
"

# 推送 tag（触发自动构建）
git push origin v1.0.0
```

### 4. 监控构建

- [ ] 访问 GitHub Actions 页面
- [ ] 确认构建成功
- [ ] 检查镜像已推送到 GHCR

### 5. 验证发布

```bash
# 拉取新镜像
docker pull ghcr.io/<你的用户名>/llmproxy:v1.0.0

# 测试运行
docker run --rm ghcr.io/<你的用户名>/llmproxy:v1.0.0 --help

# 完整功能测试
docker run -d \
  --name llmproxy-test \
  -p 8080:8080 \
  -v $(pwd)/config.yaml:/home/llmproxy/config.yaml \
  ghcr.io/<你的用户名>/llmproxy:v1.0.0

# 健康检查
curl http://localhost:8080/health

# 清理
docker stop llmproxy-test
docker rm llmproxy-test
```

### 6. 创建 GitHub Release

- [ ] 访问 `https://github.com/<你的用户名>/llmproxy/releases/new`
- [ ] 选择刚创建的 tag
- [ ] 填写 Release 标题：`v1.0.0`
- [ ] 复制 CHANGELOG 内容到描述
- [ ] 添加 Docker 使用示例：

```markdown
## 🐳 Docker 使用

\`\`\`bash
docker pull ghcr.io/<你的用户名>/llmproxy:v1.0.0
\`\`\`

支持架构：`linux/amd64`, `linux/arm64`

## 📦 完整更新日志

[查看完整 CHANGELOG](https://github.com/<你的用户名>/llmproxy/blob/main/CHANGELOG.md)
```

- [ ] 点击 `Publish release`

## 📢 发布后推广

### 社区分享
- [ ] 在项目 README 中更新版本号
- [ ] 发布到 Reddit (r/golang, r/selfhosted)
- [ ] 分享到 Hacker News
- [ ] 发布到 Twitter/X
- [ ] 更新 awesome-go / awesome-llm 列表

### 用户通知
- [ ] 在 GitHub Discussions 发布公告
- [ ] 更新项目主页（如有）
- [ ] 通知主要用户/贡献者

## 🔄 回滚流程

如果发现严重问题需要回滚：

### 1. 删除有问题的 Tag

```bash
# 删除本地 tag
git tag -d v1.0.0

# 删除远程 tag
git push origin :refs/tags/v1.0.0
```

### 2. 删除 GitHub Release

- 访问 Releases 页面
- 点击有问题的 Release
- 点击 `Delete`

### 3. 删除 Docker 镜像

- 访问 Package 页面
- 删除对应版本的镜像

### 4. 修复问题后重新发布

```bash
# 修复代码
git add .
git commit -m "fix: 修复严重问题"

# 发布修复版本
git tag v1.0.1
git push origin main
git push origin v1.0.1
```

## 📊 发布后监控

### 第一周
- [ ] 监控 GitHub Issues（新 bug 报告）
- [ ] 检查 Docker 镜像拉取次数
- [ ] 收集用户反馈

### 持续监控
- [ ] 关注 Star/Fork 增长
- [ ] 回复用户问题
- [ ] 规划下一版本功能

## 🎯 版本规划建议

### 小版本（每 2-4 周）
- Bug 修复
- 小功能改进
- 文档更新

### 中版本（每 2-3 月）
- 新功能
- 性能优化
- 依赖更新

### 大版本（每年 1-2 次）
- 架构重构
- 破坏性变更
- 重大功能

## 📝 注意事项

1. **永远不要覆盖已发布的 tag**
2. **生产环境问题优先处理**
3. **保持 CHANGELOG 更新**
4. **及时回复用户反馈**
5. **定期更新依赖（安全补丁）**
