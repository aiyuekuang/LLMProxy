package routing

import (
	"llmproxy/internal/config"
)

// RoutingConfig 路由配置（使用 config 包中的定义）
type RoutingConfig = config.RoutingConfig
type RetryConfig = config.RetryConfig
type FallbackRule = config.FallbackRule
